# Backend environment variables (example)
#
# Copy this file to `backend/.env` and replace the placeholder values.
# Notes:
# - Never commit real secrets (JWT_SECRET, production DB URIs).

PORT=5001
MONGODB_URI=mongodb://127.0.0.1:27017/medtrack
JWT_SECRET={random string here}
